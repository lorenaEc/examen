/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images:{
    domains: ['www.lorenadaniel.se', 'media4.giphy.com', 'media2.giphy.com']
  }
}

module.exports = nextConfig
